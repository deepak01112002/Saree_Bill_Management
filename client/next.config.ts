import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Port is configured via PORT environment variable or defaults to 5000
  // Set PORT in .env file or environment
};

export default nextConfig;
